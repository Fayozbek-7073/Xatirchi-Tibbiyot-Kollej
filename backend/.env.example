# Server
PORT=3000
NODE_ENV=development

# CORS — comma-separated list of allowed frontend origins
ALLOWED_ORIGINS=http://localhost:5173,https://xatirchi-tibbiyot.uz

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=xatirchi_tibbiyot
DB_USER=postgres
DB_PASSWORD=your_password_here

# JWT
JWT_SECRET=replace_with_a_long_random_secret_string
JWT_EXPIRES_IN=8h

# Bcrypt
BCRYPT_ROUNDS=12
